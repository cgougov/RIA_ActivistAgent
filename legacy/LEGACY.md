# Legacy reference (frozen)

The pre-rebuild system. Kept as a parts bin — do not extend it.
`data` and `.env` are symlinks to the repo root so old scripts remain runnable from this directory if ever needed.
Good parts already ported into the new `fund/` package: taxonomy, extraction prompts, vision return extraction, PDF pipeline, returns upsert pattern.
